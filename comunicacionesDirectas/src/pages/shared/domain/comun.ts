export type Provincia = {
  codigo: string;
  descripcion: string;
};

export type Pais = {
  codigo: string;
  descripcion: string;
};

export type TipoProcedimiento = {
  codigo: string;
  descripcion: string;
};

export type OrganoJudicial = {
  codigo: string;
  descripcion: string;
};

export type Documento = {
  idDocumento: string;
  idArchivo: string;
  titulo: string;
  fecha: string;
  tipo: string;
};
